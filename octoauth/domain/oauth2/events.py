"""
Defines events that happens on oauth2 objects, and bind listener to these events
"""

APPLICATION_CREATED = "application:created"
APPLICATION_DELETED = "application:deleted"

SCOPE_CREATED = "scope:created"
